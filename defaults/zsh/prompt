# Use starship prompt if available, otherwise a simple prompt
if command -v starship &> /dev/null; then
  eval "$(starship init zsh)"
else
  # Simple prompt with path
  PROMPT='%F{green}❯%f '
  RPROMPT='%F{240}%~%f'
fi
